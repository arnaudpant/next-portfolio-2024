export type NavLinks = {
    name: string;
    path: string
}

export type StatsType = {
    num: number;
    text: string
}

export type ServicesType = {
    num: string;
    title: string;
    description: string;
}